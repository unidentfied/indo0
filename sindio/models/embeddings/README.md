# RAG Embeddings / Vector Models

Used by the Python core for semantic search over GIS layers,
planning documents, and historical simulation results.

- Embedding model: `sentence-transformers/all-MiniLM-L6-v2`
- Stored in Qdrant collection: `sindio_embeddings`

Files in this directory are git-ignored.
