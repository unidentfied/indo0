[build]
buildCommand = "pip install -r sindio/backend/app/requirements.txt"

[deploy]
startCommand = "cd sindio/backend/app && SINDIO_SKIP_RASTER=1 PYTHONPATH=.:..: uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}"
